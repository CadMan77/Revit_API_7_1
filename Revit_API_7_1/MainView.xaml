<Window x:Class="Revit_API_7_1.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Revit_API_7_1"
             mc:Ignorable="d" 
             Height="200" Width="300" MinHeight="200" MinWidth="300"
             WindowStartupLocation="CenterScreen"
             Title="Создание листов">
    <StackPanel Margin="5">
        <Label Content="Основная надпись"/>
        <ComboBox ItemsSource="{Binding ProjectTitleBlocks}" SelectedValue="{Binding SelectedTitleBlock}" DisplayMemberPath="Name" Margin="0,0,0,20"/>
        <Label Content="Количество листов"/>
        <TextBox Text="{Binding SheetQTY}" Margin="0,0,0,20"/>
        <Button Content="   Создать листы   " HorizontalAlignment="Center" Command="{Binding CreateCommand}"/>
    </StackPanel>
</Window>
